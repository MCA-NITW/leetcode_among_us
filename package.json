{
  "name": "leetcode_among_us",
  "version": "2.0.0",
  "description": "leetcode_among_us.",
  "main": "server.mjs",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cross-env NODE_OPTIONS=\"--no-deprecation\" nodemon server.mjs",
    "dev": "concurrently \"npm run start\" \"cd client && npm start\"",
    "frontend-build": "cd client && npm run build",
    "frontend-install": "cd client && npm install",
    "build": "npm install && npm run frontend-install && npm run frontend-build",
    "format": "prettier --write ."
  },
  "author": "",
  "license": "ISC",
  "keywords": [],
  "dependencies": {
    "ag-grid-enterprise": "^35.1.0",
    "cors": "^2.8.5",
    "express": "^5.2.1",
    "express-rate-limit": "^8.2.1",
    "helmet": "^8.1.0",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "cross-env": "^10.1.0",
    "nodemon": "^3.1.14",
    "prettier": "^3.8.1"
  }
}
