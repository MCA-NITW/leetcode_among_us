{
  "name": "leetcode_among_us",
  "version": "2.0.0",
  "description": "leetcode_among_us.",
  "main": "server.mjs",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon server.mjs",
    "dev": "concurrently \"npm run start\" \"cd client && npm start\"",
    "frontend-build": "cd client && npm run build",
    "frontend-install": "cd client && npm install",
    "build": "npm install && npm run frontend-install && npm run frontend-build",
    "format": "prettier --write ."
  },
  "author": "",
  "license": "ISC",
  "keywords": [],
  "dependencies": {
    "ag-grid-enterprise": "^33.1.1",
    "cors": "^2.8.5",
    "express": "^4.21.1",
    "express-rate-limit": "^7.4.0",
    "helmet": "^8.0.0",
    "node-fetch": "^3.3.2",
    "prettier": "^3.5.2"
  },
  "devDependencies": {
    "concurrently": "^9.0.1",
    "nodemon": "^3.1.9"
  }
}
