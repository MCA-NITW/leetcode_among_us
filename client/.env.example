# Origin of the API when the client is hosted separately from the Express
# server, e.g. https://leetcode-among-us.onrender.com
# Leave empty (default) when Express serves the built client from client/dist.
VITE_API_URL=
