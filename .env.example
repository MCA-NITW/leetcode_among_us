# Server port (default: 3001)
PORT=3001

# Node environment (development | production). Render sets production.
NODE_ENV=development

# Minutes a fetched LeetCode profile is served from memory before refetching.
# Lower for fresher data, higher to be gentler on LeetCode's API. Default 10.
CACHE_TTL_MINUTES=10
